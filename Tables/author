CREATE TABLE author (
    book_id VARCHAR(200) NOT NULL ,
    author_id INT PRIMARY KEY NOT NULL,
    author_name VARCHAR(255) NOT NULL,
    nationality VARCHAR(255) NOT NULL
    book_id references sach(id1)
);